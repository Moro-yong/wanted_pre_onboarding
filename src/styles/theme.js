const size = {
  tablet: '768px',
  laptop: '992px',
  desktopSldie: '1200px',
  desktop: '1920px',
};

const theme = {
  tablet: `(max-width: ${size.tablet})`,
  laptop: `(max-width: ${size.laptop})`,
  desktopSldie: `(max-width: ${size.desktopSldie})`,
  desktop: `(max-width: ${size.desktop})`,
};

export default theme;
